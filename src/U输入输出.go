package main

func main() {

	//var i, j, k int
	//fmt.Scan(&i, &j, &k)


	//var i, j, k int
	//fmt.Scanln(&i, &j, &k)

	//var i, j int
	//var k string
	//fmt.Scanf("%d %d %s", &i, &j, &k)

	//r := bufio.NewReader(os.Stdin)  // 返回*Reader
	//s, _ := r.ReadString('\n') // 一直读，直到第一次出现‘\n’，字符串s带有‘\n’

	// 打印输入的数字
	//r := bufio.NewReader(os.Stdin)
	//for {
	//	l, _, _ := r.ReadLine()
	//	str := strings.Fields(string(l))
	//	i, _ := strconv.Atoi(str[0]) // 字符转整形（可以转负数）
	//	fmt.Println(i)
	//}

	//r := bufio.NewReader(os.Stdin)
	//for {
	//	l, _ := r.ReadString('\n')
	//	if l[0] == '\n' {
	//		break
	//	}
	//	str := strings.Fields(string(l))
	//	i, _ := strconv.Atoi(str[0]) // 字符转整形（可以转负数）
	//	fmt.Println(i)
	//}

}